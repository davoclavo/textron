Textron Neovim Client
=====================

# Install dependencies
```
npm install neovim-client
npm install git+https://git@github.com/santigimeno/node-unix-stream
```

# Run nvim with explicit socket
`NVIM_LISTEN_ADDRESS=/tmp/nvim nvim`

# Connect
`node neovim.js`
